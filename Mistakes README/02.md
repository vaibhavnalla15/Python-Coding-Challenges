# Mistakes in Challenge 2 (Even–Odd Counter)

### 1. Input prompt mismatch
- Prompt text says:  
  `"Enter comma-separated list (e.g., 1 2 3 4 5)"`  
  But the example uses spaces instead of commas.  
- Correct example should be:  
  `"Enter comma-separated list (e.g., 1, 2, 3, 4, 5)"`

### 2. Minor formatting inconsistency
- There’s an extra space before “list” in the input prompt; not a logic error, but affects polish.

### 3. Missing validation or error handling (optional improvement)
- If user enters non-numeric values or empty input, program will crash.
- Could add a `try-except` block or validation to handle such cases gracefully.

### ✅ Strengths
- Logic is perfect.
- Clean use of list comprehension and `strip()`.
- Output matches expected format.
