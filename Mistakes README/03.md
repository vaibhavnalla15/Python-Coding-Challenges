# Mistakes in Challenge 3 (Word Frequency Counter)

### 1. Hardcoded words
- You manually counted specific words (`cloud`, `engineer`, etc.).
- The program should automatically detect all unique words, not rely on known ones.

### 2. Repeated code blocks
- You repeated similar logic for each test case instead of writing a reusable program that handles any input dynamically.

### 3. Inefficient approach
- Using `sentence.lower().count("word")` repeatedly scans the entire string multiple times.
- A better approach: split the sentence into words and count using a loop or dictionary.

### 4. Punctuation handling missing
- The challenge required ignoring punctuation (`,` and `.`).
- Your code doesn’t remove them before counting.

# Mistakes in Challenge 3 (Word Frequency Counter — Your Attempt)

### 1. Variable name shadowing
- You defined `words = "cloud", "engineer", "aws"` before the loop, then reused `for words in new_sentence:`.
- This overwrote the original variable.  
  ✅ Use a different loop variable name like `for word in new_sentence:`.

### 2. Unused variable
- The tuple `words = ("cloud", "engineer", "aws")` is never used.  
  It’s unnecessary because your loop already processes all words dynamically.

### 3. Minor comment inaccuracy
- You wrote comments for correct steps but didn’t remove them after implementing; keep code clean after confirming logic.

### ✅ Strengths
- Used `.lower()` and `.replace()` correctly.
- Correct dictionary logic for counting.
- Output format (`print(word_count)`) correctly displays all word counts.



